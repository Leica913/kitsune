<!DOCTYPE html>
<html>
  <head>
    <title>Kitsune Information Room</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <script src="https://kit.fontawesome.com/22bae51414.js" crossorigin="anonymous"></script>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <!--ヘッダーの記述-->
    <header>
      <nav class="navbar navbar-dark bg-dark">
        <a href="/" class="navbar-brand">Kitsune Information Room</a>
          <div class="search-form">
            <%= form_with url: search_path, method: :get, local: true do |f| %>
              <%= f.text_field :content %>
              <%= f.select :model, options_for_select({ "ユーザー" => "user", "記事" => "book" }) %>
              <%= f.select :method, options_for_select({ "完全一致" => "perfect", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partial" }) %>
              <%= f.submit '検索' %>
            <% end %>
          </div>
        <button class="navbar-toggler" type="button"
            data-toggle="collapse"
            data-target="#navmenu1"
            aria-controls="navmenu1"
            aria-expanded="false"
            aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navmenu1">
          <div class="navbar-nav">
          <% if user_signed_in? %>
            <%= link_to 'ホーム',root_path,class: 'fas fa-home text-light' %>
            <%= link_to 'ユーザー編集',edit_user_path(current_user),class: 'fas fa-users text-light' %>
            <%= link_to 'ログアウト', destroy_user_session_path, method: :delete,class: 'fas fa-sign-out-alt text-light' %>
          <% else %>
            <%= link_to 'ホーム',root_path,class: 'fas fa-home text-light' %>
            <!--<= link_to 'About',home_about_path,class: 'fas fa-link nav-link text-light' %>-->
            <%= link_to '新規登録', new_user_registration_path,class: 'fas fa-user-plus text-light' %>
            <%= link_to 'ログイン', new_user_session_path ,class: 'fas fa-sign-in-alt text-light'%>
          <% end %>
          </div>
        </div>
      </nav>

      <!--<nav class="navbar navbar-expand-lg navbar-dark bg-dark">-->
      <!--<div class="container">-->
      <!--  <a class="navbar-brand" href="/"><span>Kitsune Information Room</span></a>-->
      <!--    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">-->
      <!--      <span class="navbar-toggler-icon"></span>-->
      <!--    </button>-->
      <!--  <div class="collapse navbar-collapse" id="navbarNavDropdown">-->
      <!--  <ul class="navbar-nav ml-auto">-->
      <!--    <div class="class: 'form-control w-50' text-center" style="margin-top: 5px; margin-right: 20px;">-->
      <!--    <= form_with url:search_path, method: :get, local: true do |f| %>-->
      <!--    <= f.text_field 'search[value]' %>-->
      <!--    <= f.select 'search[model]', options_for_select({ "ユーザー" => "user", "記事" => "book" }) %>-->
      <!--    <= f.select 'search[how]', options_for_select({ "完全一致" => "match", "前方一致" => "forward", "後方一致" => "backward", "部分一致" => "partical"  }) %>-->
      <!--    <= f.submit :"検索" %>-->
      <!--    </div>-->
      <!--    < end %>-->
      <!--    </ul>-->
      <!--  </div>-->
      <!--</div>-->
      <!--</nav>-->

    <!--<div class="row">-->
    </header>
    <!--ヘッダーの記述-->
    <div class="container">
      <div class="row">
      <p id="notice"><%= notice %></p> <!--デバイスでnotice設定されているので呼び出してるだけ-->
      </div>

      <!--<div class="row">-->
      <!--  <div class="search-box">-->
      <!--    < if user_signed_in? %>-->
      <!--       <= form_tag(search_path, :method => 'get') do %>-->
      <!--          <= text_field_tag :search %>-->
      <!--          <= select_tag(:option, options_for_select([["user", "1"], ["book", "2"]])) %>-->
      <!--          <= select_tag(:choice, options_for_select([["完全一致","1"], ["前方一致", "2"], ["後方一致", "3"], ["部分一致", "4"]]))%>-->
      <!--          <= submit_tag 'Search', :name => nil %>-->
      <!--        < end %>-->
      <!--    < else %>-->
      <!--    < end %>-->
      <!--  </div>-->
      <!--</div>-->
    </div>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
